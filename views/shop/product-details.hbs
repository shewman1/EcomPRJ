<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop</title>
    <style>
        .detail-manage{
            display: flex;
        }
        .left{
            margin-top: 50px;
            width: 35%;
            margin-right: 10px;
        }
        .left img{
            width: 100%;
            height: 100%;
        }
        .right{
            width: 70%;
            margin-left: 10px;
            margin-top: 50px;
        }
        .right-baat{
            padding: 10px;
        }
        .fontr{
            font-size: 24px;
        }
        .btn{
            text-decoration: none;
            width: 130px;
            height: 65px;
            margin: 20px 20px 20px 40px;
            font-size: 18px;
            background-color: rgb(244, 238, 238);
        }
    </style>
    {{>generalcss}}
</head>
<body>
    {{>navbar}}
    <div class="product-item">
        <div class="my-product-image">
            <div class="detail-manage">
                <div class="left">
                    <img src="{{product.imageUrl}}">
                </div>
                <div class="right">
                    <h4 class="right-baat fontr">{{product.name}}</h4>
                    <h4 class="right-baat fontr">{{product.price}}</h4>
                    <h4 class="right-baat">Product Description:{{product.description}}</h4>
                    <h4 class="right-baat fontr">Product Seller:{{product.seller}}</h4>
                    <h4 class="right-baat">Reviews: </h4>
                    <ul>
                        {{#each product.review as |r|}}
                            <li>{{r}}</li>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
        <a style="text-decoration: none;" href="/shop/cart/add/{{product._id}}">
            <button class="btn">Add to Cart</button>
        </a>
        <a style="text-decoration: none;" href="#">
            <button class="btn">Buy Now</button>
        </a>
</body>
</html>