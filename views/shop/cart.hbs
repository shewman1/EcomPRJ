<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart</title>
    {{>generalcss}}

    <style>
        .cart-list {
            width: 800px;
            margin: auto;
            list-style: none;

        }

        .cart-list-item {
            display: flex;
            align-items: center;
        }

        .increaseQuantity a:link,
        .increaseQuantity a:visited,
        .decreaseQuantity a:visited,
        .decreaseQuantity a:link {
            text-decoration: none;
            color: black;
        }

        .buyNowBtn {
            padding: 10px;
            border-radius: 10px;
            border: none;
            background-color: #000;
            color: #fff;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
        }
        .cart-data{
            margin-left: 50px;
        }
        .cart-data p{
            font-size: 22px;
            font-weight: 200;
        }
        .cart-data div{
            font-size: 22px;
            font-weight: 200;
        }
    </style>
</head>

<body>

    {{>navbar}}

    <h1>Welcome to cart page</h1>
    <ul class="cart-list">
        {{#each cart as |c|}}
        <li class="cart-list-item">
            <div class="cart-img">
                <img src={{c.id.imageUrl}} alt="" width="200px" height="200px">
            </div>
            <div class="cart-data">
                <p>Name: {{c.id.name}}</p>
                <p>Price: {{c.id.price}}</p>

                <div>
                    Quantity:
                    <a href="/shop/cart/increase/{{c.id._id}}">&nbsp;
                        <button class="increaseQuantity">
                            +
                        </button>
                    </a>&nbsp;{{c.quantity}}&nbsp;
                    <a href="/shop/cart/decrease/{{c.id._id}}">
                        <button class="decreaseQuantity">
                            -
                        </button>
                    </a>
                    <div id={{c.id._id}}></div>
                </div>
            </div>

        </li>
        {{/each}}
    </ul>
    {{!-- <p style="font-size: 22px;"><b>Total Price:</b> <span class="totalPrice">{{totalPrice}}</span></p> --}}



</body>

</html>